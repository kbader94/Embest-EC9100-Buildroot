config BR2_PACKAGE_FC_TESTS
    bool "fc_tests (FIFO Control tests)"
    depends on BR2_LINUX_KERNEL  
    help
      FIFO Control test suite (fc_tests).
      Installs kernel modules and the rtt_tests user binary.
